---
title: "Untitled"
author: "Collin Guedel"
date: "`r Sys.Date()`"
output:
   bookdown::pdf_document2:
    template: thesis-for-rmd.tex
    toc: true
    lof: true
    keep_tex: true
    extra_dependencies: ["float"]
    number_sections: False
    fig_caption: True
    fig_width: 6
    fig_height: 4
    citation_package: natbib
header-includes:
  - \setlength\parindent{24pt}\setlength{\parskip}{0.0pt plus 1.0pt}
  - \usepackage{indentfirst}
  - \usepackage{fvextra}
  - \usepackage{float}
  - \usepackage{placeins}
  - \DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines=true, breakanywhere=true, commandchars=\\\{\}}
  - \usepackage{setspace}
  - \linespread{1.5}
  - \usepackage[nottoc]{tocbibind}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars, fig.cap = "huh"}
summary(cars)
knitr::kable(head(cars), caption = "cars head test")
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE, fig.cap = "plot of pressure test"}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
